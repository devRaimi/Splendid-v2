#include <math.h>
#include <stdio.h>
int main() {
    double sine, cosine, num;
    // for ( i = 0; i > 0; i++)
    // {
        scanf("%lf", num);
        sine = cos(num);
        // cosine = cos(num);
        printf("The sine of the entered number is %lf", sine);

    // }
    return 0;
    

}